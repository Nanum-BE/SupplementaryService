server:
  port: 8088
#logging:
#  level:
#    org.springframework: DEBUG

spring:
  application:
    name: supplementary-service
  profiles:
    include: mysql, swagger, s3
  messages:
    basename: messages
  zipkin:
    base-url: http://20.214.170.222:9411
    enabled: true
  sleuth:
    sampler:
      probability: 1.0
  rabbitmq:
    port: 5672
    username: guest
    password: guest
    host: 20.214.170.222
#spring.cloud.client.hostname
eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://20.214.170.222:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info, httptrace, metrics, prometheus


